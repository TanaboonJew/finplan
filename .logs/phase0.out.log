Environment is ready (Node 24, npm 11, git). Scaffolding Next.js into a temp folder first since `create-next-app` refuses non-empty directories:
Template inspected (Next 16, Tailwind v4, typed route helpers). Merging into the repo now:
Install didn't take (package.json unchanged). Re-running explicitly:
All deps in (next-intl 4.13, lucide icons verified). Per AGENTS.md, checking the bundled Next.js docs for API changes before writing code:
Docs confirm two critical changes: **middleware → proxy** (Next 16) and root layout allowed under `[locale]`. Now writing the config/i18n layer:
Now generating the 18 tool route placeholders (identical boilerplate, so scripted):
All files in place. Running the verification suite:
Lint flags the classic `mounted` pattern under the new React hooks rules. Switching to a hydration-safe CSS-based icon swap instead:
Two cleanups: RTL auto-cleanup isn't active without vitest globals (leaks DOM between tests), and Vitest suggests its native tsconfig-paths option:
Tests green. Now the production build:
PowerShell 5.1's `Set-Content -Encoding UTF8` wrote BOMs, which break JSON parsing. Stripping BOMs from every file I touched via PowerShell:
`ReadAllText` auto-strips BOM on decode, so that check was useless. Byte-level scan instead across the whole repo:
BOM is gone — the failure is a stale Turbopack cache. Clearing `.next` and rebuilding:
RSC boundary issue: `ToolMeta.icon` passes a component function from server to client. Moving to a string-keyed icon registry resolved inside the client card:
Build succeeds — all routes SSG'd per locale with proxy active. Final verification pass after the refactor:

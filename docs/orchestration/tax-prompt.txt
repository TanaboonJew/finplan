Read MASTER-PLAN.md first for context and section 2 rules, but DO NOT edit MASTER-PLAN.md itself - the orchestrator updates statuses. PARALLEL-SAFETY RULES: another agent will work on a different tool later - keep to your own files so merges stay clean. Touch ONLY: your tool route folder src/app/[locale]/(tools)/tax/, new components under src/components/tools/tax/, your zustand store src/lib/storage/tax-store.ts, demo data src/lib/demo/tax.ts, spec docs/specs/tax.md, and i18n keys under YOUR OWN top-level "tax" namespace in src/messages/en.json and th.json. Never modify shared components in src/components/shared or ui, never touch other tools folders or files. For finance math: reuse existing src/lib/finance modules; if you need new functions, add NEW uniquely-named files or additive new exports only if unavoidable - never rewrite existing ones. Never edit package.json unless a dep is truly missing. Write docs/specs/tax.md BEFORE coding. When done: lint/typecheck/test green, then git add -A and commit on your current branch. You are executing the Tax planner per MASTER-PLAN section 6 row 6 - tax.ts bracket engine exists; take-home breakdown UI, deductions, TH and US bracket presets as the improvement.